1. 如何将OS的CPU（%）和Memory（物理+交换+实际物理占用+占用交换分区的大小（K，M，G））在一张图内显示好？

    一台机器的初始物理大小+交换大小，一般不变，可以将图的最大值限定死，底色显示。
    实际物理占用量，采用柱状显示。实际交换分区占用量也采用柱状图，
    
    
2. 如何监控某个进程CPU（%）和Memory（具体占用量，K，M，G）使用


3. OS的IO可以直接使用MRTG的原则。



4. Ping或TCP Ping

  a). 是否通（0/1）
  b). 如果是广域网，ping有延时的。柱状，柱高表示延时值

5. HTTP请求，延迟，柱高表示延时值，与Ping类似。
6. SDS复制队列监控，0~很大


